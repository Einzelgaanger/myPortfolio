# Render Blueprint - new Web Service from this repo
# After connecting this repo, Render will use these settings.
services:
  - type: web
    name: alfred-portfolio
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    # Static files + SPA fallback served from dist/public by Node server
